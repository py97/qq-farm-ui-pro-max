# 遗留项与后续行动配置说明 (TODO)

经过本次大规模吸纳与功能整合，我们解决了诸多痛点并保留了核心优势，但为了进一步的极致体验，仍然留有一些值得关注的长期维护点。

## 遗留项清单 (TODO)

### 1. TODO: 移动端 (Mobile) UI 彻底适配微调
- **描述**：虽然已经处理过 `Settings.vue`、`Analytics.vue` 的列网格布局响应式 (`md:grid-cols-2` 等)，但在一些极端窄屏手机上，新增的“运维说明”、“多策略卡片”等仍有轻微拥挤的可能性。
- **建议动作**：后续日常可以开启浏览器的设备模拟器，对于部分组件应用稍微小一号的字体（text-xs），或为卡片增加横向滚动条（overflow-x-auto）。

### 2. TODO: 腾讯云端/宝塔面板的用户测试验证
- **描述**：加入的 7 个 Shell 部署脚本在 Mac 和主流 Linux 系统上进行了逻辑编写，但在各种魔改 Linux (如 CentOS 老版本) 或带有强限制的宝塔面板环境下可能存在权限兼容差异。
- **建议动作**：在下次上新服务器部署时，针对 `start-with-screen.sh` 和 `install-systemd-service.sh` 试水跑一次，观察是否畅通无阻，如遇到相关无权限问题可加 `sudo` 执行。

### 3. TODO: `common.js` 降级容错组件的扩展应用观测
- **描述**：我们在此次优化中成功拦下了核心抢收包的 `withRetry` 改造，避免了卡点死锁，这是一件具有战略意义的好事。但这并不代表 `common.js` 毫无用武之地。
- **建议动作**：在未来扩展“登录腾讯接口”、“拉取商城道具列表”等**非时间敏感**且容易因网络颠簸而抛出断连的 API 时，继续尝试引入 `withRetry`。

## 您下一步可以做的：
1. **执行系统全量编译**：运行一次 `pnpm build:web` 然后再重新跑一遍本地起服，在真实浏览器中快速点一遍。
2. **打包封版**：没有问题之后，这将会是一个非常完美的长期养老版 (v4.2.0)。
3. 目前 `FINAL` 和 `TODO` 文档已归档完毕。您可以查看本文档或直接告知您的下一步决策！
